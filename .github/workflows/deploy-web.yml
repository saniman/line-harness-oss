name: Deploy Web to Cloudflare Pages

on:
  push:
    branches: [main]
    paths:
      - 'apps/web/**'
      - 'packages/**'
      - '.github/workflows/deploy-web.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm --filter @line-crm/shared build

      - name: Type check
        run: pnpm --filter web exec tsc --noEmit

      - run: pnpm --filter web run build
        env:
          NEXT_PUBLIC_API_URL: https://api.walover-co.work
          NEXT_PUBLIC_API_KEY: ${{ secrets.API_KEY }}

      - name: Deploy to Cloudflare Pages
        run: pnpm --filter worker exec wrangler pages deploy apps/web/out --project-name=line-harness-web --account-id=aa7b3de8b8dc8dd263572893aa4f652e
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
